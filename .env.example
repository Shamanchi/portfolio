# Copy to .env for local runs. Never commit .env.
# CI runs without these keys; network steps opt in when a key is present.

OPENAI_API_KEY=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AZURE_SUBSCRIPTION_ID=