{
  "name": "p17-aks-azure-devops",
  "version": "1.0.0",
  "private": true,
  "description": "Original AKS + Azure DevOps delivery demo: an Azure Pipelines renderer, an AKS readiness engine and the corresponding az/helm cheat sheet.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p .",
    "test": "node --test \"tests/**/*.test.ts\"",
    "verify": "npm run typecheck && npm test",
    "cli": "node src/cli.ts",
    "check": "node src/cli.ts --check",
    "gate": "npm run verify && npm run check"
  },
  "devDependencies": {
    "@types/node": "^22.20.4",
    "typescript": "^7.0.2"
  }
}
