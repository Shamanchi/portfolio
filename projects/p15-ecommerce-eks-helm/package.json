{
  "name": "p15-ecommerce-eks-helm",
  "version": "1.0.0",
  "private": true,
  "description": "Original e-commerce EKS + Helm delivery demo: an app values model, a Helm chart with a minimal template renderer and an EKS release plan.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p .",
    "test": "node --test \"tests/**/*.test.ts\"",
    "verify": "npm run typecheck && npm test",
    "cli": "node src/cli.ts",
    "check": "node src/cli.ts --check",
    "gate": "npm run verify && npm run check"
  },
  "devDependencies": {
    "@types/node": "^22.20.4",
    "typescript": "^7.0.2"
  }
}
