{
  "name": "p11-aws-2tier-terraform",
  "version": "1.0.0",
  "private": true,
  "description": "AWS two-tier architecture as code: web instances behind a public subnet and a database in a private subnet, rendered to deterministic Terraform HCL.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p .",
    "test": "node --test \"tests/**/*.test.ts\"",
    "verify": "npm run typecheck && npm test",
    "cli": "node src/cli.ts",
    "check": "node src/cli.ts --check"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "typescript": "^7.0.2"
  }
}