{
  "name": "p18-jenkins-java-full-cicd",
  "version": "1.0.0",
  "private": true,
  "description": "Original full Jenkins CI/CD for a Java service: Maven quality gates, docker tag rules and a declarative Jenkinsfile renderer.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p .",
    "test": "node --test \"tests/**/*.test.ts\"",
    "verify": "npm run typecheck && npm test",
    "cli": "node src/cli.ts",
    "check": "node src/cli.ts --check",
    "gate": "npm run verify && npm run check"
  },
  "devDependencies": {
    "@types/node": "^22.20.4",
    "typescript": "^7.0.2"
  }
}
