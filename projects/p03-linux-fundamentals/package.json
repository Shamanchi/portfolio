{
  "name": "p03-linux-fundamentals",
  "version": "1.0.0",
  "private": true,
  "description": "Linux fundamentals practice: lessons, self-checked quiz and a lab command reference. Runs on Windows.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p .",
    "test": "node --test \"tests/**/*.test.ts\"",
    "verify": "npm run typecheck && npm test",
    "cli": "node src/cli.ts",
    "self-check": "node src/cli.ts --self-check"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "typescript": "^7.0.2"
  }
}
