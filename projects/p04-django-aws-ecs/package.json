{
  "name": "p04-django-aws-ecs",
  "version": "1.0.0",
  "private": true,
  "description": "Deploy a containerized Django app to AWS ECS Fargate: model, task definition renderer and Terraform output generator.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p .",
    "test": "node --test \"tests/**/*.test.ts\"",
    "verify": "npm run typecheck && npm test",
    "cli": "node src/cli.ts",
    "check": "node src/cli.ts --check"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "typescript": "^7.0.2"
  }
}
