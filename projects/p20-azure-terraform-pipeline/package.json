{
  "name": "p20-azure-terraform-pipeline",
  "version": "1.0.0",
  "private": true,
  "description": "Original Azure IaC demo: an azurerm Terraform stack with a plan-driven Azure DevOps pipeline and a tfstate backend-key engine.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p .",
    "test": "node --test \"tests/**/*.test.ts\"",
    "verify": "npm run typecheck && npm test",
    "cli": "node src/cli.ts",
    "check": "node src/cli.ts --check",
    "gate": "npm run verify && npm run check"
  },
  "devDependencies": {
    "@types/node": "^22.20.4",
    "typescript": "^7.0.2"
  }
}
