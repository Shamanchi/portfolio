{
  "name": "p14-github-actions-android",
  "version": "1.0.0",
  "private": true,
  "description": "Original Android CI/CD pipeline demo: semver + git-describe versioning, an offline Android lint model and a release-safe GitHub Actions workflow generator.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p .",
    "test": "node --test \"tests/**/*.test.ts\"",
    "verify": "npm run typecheck && npm test",
    "cli": "node src/cli.ts",
    "check": "node src/cli.ts --check",
    "gate": "npm run verify && npm run check"
  },
  "devDependencies": {
    "@types/node": "^22.20.4",
    "typescript": "^7.0.2"
  }
}
