{
  "name": "p01-java-aws-3tier",
  "version": "1.0.0",
  "private": true,
  "description": "Three-tier architecture as code: typed model, validation, Terraform HCL rendering.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p .",
    "test": "node --test \"tests/**/*.test.ts\"",
    "verify": "npm run typecheck && npm test",
    "cli": "node src/cli.ts"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "typescript": "^7.0.2"
  }
}
