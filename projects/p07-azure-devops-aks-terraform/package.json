{
  "name": "p07-azure-devops-aks-terraform",
  "version": "1.0.0",
  "private": true,
  "description": "Generate an AKS cluster with Terraform (azurerm) and a guarded Azure DevOps pipeline with plan/apply and an approval environment.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p .",
    "test": "node --test \"tests/**/*.test.ts\"",
    "verify": "npm run typecheck && npm test",
    "cli": "node src/cli.ts",
    "check": "node src/cli.ts --check"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "typescript": "^7.0.2"
  }
}
