{
  "name": "p06-advanced-cicd-pipeline",
  "version": "1.0.0",
  "private": true,
  "description": "Model an advanced CI/CD pipeline with quality gates, artifact scan, SBOM and approval-protected environments, then render GitHub Actions workflows.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p .",
    "test": "node --test \"tests/**/*.test.ts\"",
    "verify": "npm run typecheck && npm test",
    "cli": "node src/cli.ts",
    "check": "node src/cli.ts --check"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "typescript": "^7.0.2"
  }
}
